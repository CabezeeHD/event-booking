apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: event-booking
spec:
  selector:
    app: eventservice
  ports:
    - name: http
      port: 3002
      targetPort: 3002
    - name: grpc
      port: 50051
      targetPort: 50051
  type: ClusterIP
